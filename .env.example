# Copy to .env.local and fill in. NEVER commit .env.local.

# === App ===
NEXT_PUBLIC_APP_URL=http://localhost:3000

# === Supabase (managed) ===
# Get from https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# === Resend (transactional email) ===
# Already provisioned. Key in /mnt/datos/eu-ai-act-saas/infra/docker/.env.secrets on NAS.
RESEND_API_KEY=
RESEND_FROM_DEFAULT=hola@iactready.com

# === Anthropic Claude (LLM for risk classification + doc generation) ===
ANTHROPIC_API_KEY=

# === Cloudflare (DNS automation — admin scripts only, not used at runtime) ===
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=6cb0d47707f5bdb30f432087908a3cf1
